name: rpmbuild
description: Build RPM
requirements:
  - rpmbuild
configurations:
  properties:
    spec:
      type: string
    share:  
      type: string
    name:
      type: string
  required: 
    - name
    - spec
		- share
